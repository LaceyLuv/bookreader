{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main WebView to invoke application commands without exposing plugin or core permissions.",
  "windows": [
    "main"
  ],
  "permissions": []
}
